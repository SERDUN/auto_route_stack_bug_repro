name: test_autorouter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Або ваша версія SDK

dependencies:
  flutter:
    sdk: flutter
  auto_route: 9.3.0+1
  flutter_bloc: ^8.1.3
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  auto_route_generator: 9.3.1
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
